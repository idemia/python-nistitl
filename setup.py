#!/usr/bin/env python

from setuptools import setup

setup (name = 'nistitl',
    version = '0.1',
    description = 'NIST-ITL Python Parsing Library',
    py_modules = ['nistitl'],
    test_suite = 'tests',
    )
